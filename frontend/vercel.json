{
  "buildCommand": "export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter build web --release --web-renderer html",
  "outputDirectory": "build/web",
  "installCommand": "if [ -d \"flutter\" ]; then echo \"Flutter SDK already installed.\"; else git clone https://github.com/flutter/flutter.git -b stable; fi && export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter pub get",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    }
  ]
}
